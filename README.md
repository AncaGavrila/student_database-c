# student_database-c
# Student Database Management System (C)

A console-based student management system written in C, designed for educational purposes.  
This project demonstrates core concepts of the C programming language, including dynamic memory allocation, file handling, modular programming, data structures, and basic algorithms.

---

## 🚀 Features

- Add new students (ID, name, grade)
- Delete students by ID
- Display all students
- Search students by name (substring matching)
- Sort students by grade (descending)
- Save database to file
- Load database from file at startup
- Uses dynamic memory (malloc / realloc / free)
- Modular structure (`main.c`, `student.c`, `student.h`)

---
